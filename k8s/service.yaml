apiVersion: v1
kind: Service
metadata: 
  name: fcg-notifications-worker
spec:
  selector:
    app: fcg-notifications-worker

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  
  type: ClusterIP